import { Options } from 'select2';

type Select2PropsType = {
  data?: any,
  name?: string,
  value?: any,
  options?: Options,
  className?: string,
  additionalClassName?: string,
}

export { Select2PropsType };
